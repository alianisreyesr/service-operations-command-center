# Contributing

Use fictional data only. Keep domain rules independent of HTTP handlers, add tests for behavior changes, run `python -m pytest`, and document new API contracts or limitations before opening a pull request.
